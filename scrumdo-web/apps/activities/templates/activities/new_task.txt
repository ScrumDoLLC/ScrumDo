{% load markup %}{% load projects_tags %}
{% with task.story as story %}
created task {% if task.assignee %}assigned to <i>{{task.assignee}}</i>{% endif %} - {{task.summary}}
in story <a href="{{story.get_absolute_url}}">#{{story.local_id}}</a> in <a href="{% url iteration story.iteration.project.slug story.iteration.id %}">{{story.project.name}} / {{story.iteration.name}}</a>. 
{% endwith %}